format_version = 1
name = "SCurve"
pkgname = "VelocityProfile::SCurve"
version = "0.1.0"
dependencies = ["VelocityProfile::Core"]
ignore = ["simulation"]

[compile]
options = ["-Ofast", "-ffast-math"]
defines = ["ARM_MATH_CM4"]
